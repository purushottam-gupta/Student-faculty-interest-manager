<!DOCTYPE html>

<html>

<head>
    <title>The Mediator | IIIT Una/Edit</title>
    <link rel="shortcut icon" href="https://www.pngitem.com/pimgs/b/191-1914071_handshake-icon-png.png">

   <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css" integrity="sha384-B0vP5xmATw1+K9KRQjQERJvTumQW0nPEzvF6L/Z6nronJ3oUOFUFpCjEUQouq2+l" crossorigin="anonymous">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
</head>

<body class="bg-info container-fluid">
    <div class="row">
        <div class="col-md-8 offset-md-2" style="background-color: #fff;margin-top: 25px;padding:20px;">
            <h3>{{viewTitle}}</h3>

            <form action="/faculty" method="POST" autocomplete="off" enctype="multipart/form-data">
                <input type="hidden" name="_id" value="{{faculty._id}}">
                <div class="form-group">
                    <label>Profile picture</label>
                    <input type="file" class="form-control" name="avatar">
                    <div class="text-danger">
                        {{error}}</div>
                </div>
                <div class="form-group">
                    <label>Full Name</label>
                    <input type="text" class="form-control" name="fullName" placeholder="Full Name"
                        value="{{faculty.fullName}}">
                    <div class="text-danger">
                        {{faculty.fullNameError}}</div>
                </div>
                <div class="form-group">
                    <label>Email</label>
                    <input type="text" class="form-control" name="email" placeholder="Email" value="{{faculty.email}}">
                    <div class="text-danger">
                        {{faculty.emailError}}
                    </div>
                </div>
                <div class="form-row">
                    <div class="form-group col-md-6">
                        <label>Entry</label>
                        <input type="number" class="form-control" name="entry" placeholder="max. number of students accepting"
                            value="{{faculty.entry}}" min="0" max="9">
                            <div class="text-danger">
                        {{faculty.entryError}}</div>
                    </div>
                    <div class="form-group col-md-6">
                        <label>Field of Interest</label>
                        <input type="text" class="form-control" name="fieldOfInterest" placeholder="Interest"
                            value="{{faculty.fieldOfInterest}}">
                            <div class="text-danger">
                        {{faculty.fieldOfInterestError}}</div>
                    </div>
                </div>
                <p class="text-danger font-italic">{{message}}</p>
                <div class="form-group">
                    <button type="submit" class="btn btn-info"><i class="fa fa-database"></i> Submit</button>
                    <a class="btn btn-secondary" href="/faculty/list"><i class="fa fa-list-alt"></i> View All</a>
                </div>
            </form>
        </div>
    </div>
</body>

</html>